node_modules/
dist/
bundles/
*.tsbuildinfo
.DS_Store
.cc-audit/

# Local calibration artifacts — contain personal task gists, never commit
/cc-audit-labels.json
/relabeled.json
/scored.json

# Local MCP server config (personal tooling, not project-wide)
/.mcp.json
